package sample.util;

import java.util.PriorityQueue;

public class PointPriorityQueue<e> extends PriorityQueue<PointDistance> {
    @Override
    public boolean remove(Object o) {
        return super.remove(o);
    }
}
